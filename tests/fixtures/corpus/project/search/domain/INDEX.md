# Fixture domain sub-index

A sub-index defines its own membership from the rows already in it, so the
first row is hand-seeded and `--write` regenerates only what is listed here.

## Index

- [turbine_balancing](turbine_balancing.md) — Turbine balancing weights are recorded per rotor, because a swapped rotor carries its own imbalance and not the housing one.
